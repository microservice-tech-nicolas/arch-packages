# Maintainer: Nicolas <nicolas.verhaert@microservice.tech>
pkgname=nic-dotfiles
pkgver=1
pkgrel=9
pkgdesc="Nicolas dotfiles — bare git clone into $HOME (includes nvim config)"
arch=('any')
url="https://github.com/microservice-tech-nicolas/arch-dotfiles"
license=('MIT')
depends=('git')
install=${pkgname}.install
source=()
sha256sums=()

package() {
  # All work happens via the .install hook at post_install time
  true
}
